import styled from "@emotion/styled";

const CreactTaskModal = () => {
    return(
        <Background>

        </Background>
    )
}

const Background = styled.div`
    width: 100vw;
    height: 100vh;
    background-color: rgb(0, 0, 0, 0.5);
`;

export default CreactTaskModal;